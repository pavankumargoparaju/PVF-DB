@model IEnumerable<S1G2_PVFAPP.Models.Work_center>

@{
    ViewBag.Title = "Index";
}
<style type="text/css">

    .table {
        width: 100%;
        border-collapse: collapse;
    }

        .table th {
            background: #527EA9;
        }

        /* provide some minimal visual accomodation for IE8 and below */

        .table tr {
            background: #b8d1f3;
        }



            /* ODD background rows */

            .table tr:nth-child(odd) {
                background: #b8d1f3;
            }

            /* EVEN background rows */

            .table tr:nth-child(even) {
                background: #dae5f4;
            }
</style>
<h2>Work Center Location</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WorkcenterID)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.WorkcenterName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WorkcenterID)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.WorkcenterName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.WorkcenterID }) |
                @Html.ActionLink("Details", "Details", new { id = item.WorkcenterID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.WorkcenterID })
            </td>
        </tr>
    }

</table>
